import { PageWrapper } from '@/components/PageWrapper';
import { StocksList } from '@/components/accounts/StocksList';

export function StocksPage() {
  return (
    <PageWrapper>
      <StocksList />
    </PageWrapper>
  );
}
